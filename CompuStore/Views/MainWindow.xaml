<Window x:Class="CompuStore.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:infrastrucure="clr-namespace:CompuStore.Infrastructure;assembly=CompuStore.Infrastructure"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        prism:ViewModelLocator.AutoWireViewModel="True"        
        Title="CompuStore" Height="600" Width="800" WindowStartupLocation="CenterScreen"        
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Icon="/CompuStore;component/MainIcon.ico">

    <DockPanel LastChildFill="True">
        <DockPanel.Background>
        <RadialGradientBrush>
            <GradientStop Color="#FF80B3D4" />
            <GradientStop Color="#FF456AB4"
                              Offset="1" />
        </RadialGradientBrush>
        </DockPanel.Background>
        <Image Width="auto" Stretch="Fill" Height="150" Source="{StaticResource Logo}" DockPanel.Dock="Top"></Image>
        <Border  Width="200"  BorderBrush="#FF4355C1"
                BorderThickness="2,2,2,2"
                CornerRadius="4,4,4,4"
                Margin="8,2,2,8" DockPanel.Dock="Left" >
           <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Button Command="{Binding SalesCommand}" Grid.Row="0" Height="Auto" Style="{DynamicResource MaterialDesignRaisedButton}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <materialDesign:PackIcon Margin="0,0,20,0" Kind="Sale" Width="40" Height="40" Foreground="Lime"/>
                            <TextBlock VerticalAlignment="Center" ><Run Text="Sales"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button Command="{Binding PurchasesCommand}" Grid.Row="1" Height="Auto" Style="{DynamicResource MaterialDesignRaisedButton}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <materialDesign:PackIcon Margin="0,0,20,0" Kind="Sale" Width="40" Height="40" Foreground="Red"/>
                            <TextBlock VerticalAlignment="Center" ><Run Text="Purchases"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button Command="{Binding StoreCommand}" Grid.Row="2" Height="Auto" Style="{DynamicResource MaterialDesignRaisedButton}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <materialDesign:PackIcon Margin="0,0,20,0" Kind="Store" Width="40" Height="40" Foreground="Yellow"/>
                            <TextBlock VerticalAlignment="Center" ><Run Text="Store"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button Command="{Binding ClientsCommand}" Grid.Row="3" Height="Auto" Style="{DynamicResource MaterialDesignRaisedButton}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <materialDesign:PackIcon Margin="0,0,20,0" Kind="Account" Width="40" Height="40" Foreground="Lime"/>
                            <TextBlock VerticalAlignment="Center" ><Run Text="Clients"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button Command="{Binding SuppliersCommand}" Grid.Row="4" Height="Auto" Style="{DynamicResource MaterialDesignRaisedButton}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <materialDesign:PackIcon Margin="0,0,20,0" Kind="City" Width="40" Height="40" Foreground="Red"/>
                            <TextBlock VerticalAlignment="Center" ><Run Text="Suppliers"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button Command="{Binding ReportsCommand}" Grid.Row="5" Height="Auto" Style="{DynamicResource MaterialDesignRaisedButton}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <materialDesign:PackIcon Margin="0,0,20,0" Kind="BookMultiple" Width="40" Height="40" Foreground="Yellow"/>
                            <TextBlock VerticalAlignment="Center" ><Run Text="Reports"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </Grid>
        </Border>
        <Border  BorderBrush="#FF4355C1"
                BorderThickness="2,2,2,2"
                CornerRadius="4,4,4,4"
                Margin="8,2,2,8">
            <materialDesign:TransitioningContent  OpeningEffect="{materialDesign:TransitionEffect Kind=FadeIn}" prism:RegionManager.RegionName="{x:Static infrastrucure:RegionNames.MainContentRegion}"/>
        </Border>
    </DockPanel>
</Window>
