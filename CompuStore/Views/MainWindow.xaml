<Controls:MetroWindow x:Class="CompuStore.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:infrastrucure="clr-namespace:CompuStore.Infrastructure;assembly=CompuStore.Infrastructure"
        prism:ViewModelLocator.AutoWireViewModel="True"        
        Title="CompuStore" Height="600" Width="800" WindowStartupLocation="CenterScreen"        
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        GlowBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="2"  ResizeMode="CanResizeWithGrip" WindowTransitionsEnabled="False"                      
        >
    <Controls:MetroWindow.IconTemplate>
        <DataTemplate>
            <Grid Width="{TemplateBinding Width}"
                 Height="{TemplateBinding Height}">
                <iconPacks:PackIconOcticons Kind="DeviceDesktop" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" />
            </Grid>
        </DataTemplate>
    </Controls:MetroWindow.IconTemplate>
    <DockPanel LastChildFill="True">        
        <Image Width="auto" Stretch="Fill" Height="150" Source="{StaticResource Logo}" DockPanel.Dock="Top"></Image>
        <Border  Width="200"  BorderBrush="#FF4355C1"
                BorderThickness="2,2,2,2"
                CornerRadius="4,4,4,4"
                Margin="8,2,2,8" DockPanel.Dock="Left" >
           <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Button BorderThickness="0" Command="{Binding SalesCommand}" Grid.Row="0" Height="Auto" Style="{DynamicResource AccentedSquareButtonStyle}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <iconPacks:PackIconModern Margin="0,0,20,0" Kind="CurrencyDollar"/>
                            <TextBlock VerticalAlignment="Center" ><Run Text="Sales"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button BorderThickness="0" Command="{Binding PurchasesCommand}" Grid.Row="1" Height="Auto" Style="{DynamicResource AccentedSquareButtonStyle}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <iconPacks:PackIconModern Margin="0,0,20,0"  Kind="Forsale" />
                            <TextBlock VerticalAlignment="Center" ><Run Text="Purchases"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button BorderThickness="0" Command="{Binding StoreCommand}" Grid.Row="2" Height="Auto" Style="{DynamicResource AccentedSquareButtonStyle}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <iconPacks:PackIconModern Margin="0,0,20,0"  Kind="Store" />
                            <TextBlock VerticalAlignment="Center" ><Run Text="Store"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button BorderThickness="0" Command="{Binding ClientsCommand}" Grid.Row="3" Height="Auto" Style="{DynamicResource AccentedSquareButtonStyle}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <iconPacks:PackIconModern Margin="0,0,20,0" Kind="UserTie" />
                            <TextBlock VerticalAlignment="Center" ><Run Text="Clients"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button BorderThickness="0" Command="{Binding SuppliersCommand}" Grid.Row="4" Height="Auto" Style="{DynamicResource AccentedSquareButtonStyle}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <iconPacks:PackIconMaterial Margin="0,0,20,0" Kind="Domain" />
                            <TextBlock VerticalAlignment="Center" ><Run Text="Suppliers"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button BorderThickness="0" Command="{Binding ReportsCommand}" Grid.Row="5" Height="Auto" Style="{DynamicResource AccentedSquareButtonStyle}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Width="150">
                            <iconPacks:PackIconMaterial Margin="0,0,20,0" Kind="FileChart" />
                            <TextBlock VerticalAlignment="Center" ><Run Text="Reports"/></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </Grid>
        </Border>
        <Border  BorderBrush="#FF4355C1"
                BorderThickness="2,2,2,2"
                CornerRadius="4,4,4,4"
                Margin="8,2,8,8">
            <Controls:TransitioningContentControl Transition="LeftReplace"  prism:RegionManager.RegionName="{x:Static infrastrucure:RegionNames.MainContentRegion}"/>
        </Border>
    </DockPanel>
</Controls:MetroWindow>
