<Controls:MetroWindow x:Class="CompuStore.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:infrastrucure="clr-namespace:CompuStore.Infrastructure;assembly=CompuStore.Infrastructure"
        prism:ViewModelLocator.AutoWireViewModel="True"        
        Title="CompuStore" Height="700" Width="1000" WindowStartupLocation="CenterScreen"        
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        GlowBrush="{DynamicResource AccentColorBrush}"
                      WindowState="Maximized"
                      
        BorderThickness="2"  ResizeMode="CanResizeWithGrip" WindowTransitionsEnabled="False" Icon="/CompuStore;component/MainIcon.ico"                      
        >
    <Window.Resources>
        <Style x:Key="defaultStyle" TargetType="Button" BasedOn="{StaticResource AccentedSquareButtonStyle}" >
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="75"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"></Setter>
            <Setter Property="Opacity" Value="1"/>
            <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="{DynamicResource AccentColorBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="activeStyle" TargetType="Button" BasedOn="{StaticResource defaultStyle}" >
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="OrangeRed"></Setter>
        </Style>
    </Window.Resources>
    <Controls:MetroWindow.IconTemplate>
        <DataTemplate>
            <Grid Width="{TemplateBinding Width}"
                 Height="{TemplateBinding Height}">
                <iconPacks:PackIconOcticons Kind="DeviceDesktop" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" />
            </Grid>
        </DataTemplate>
    </Controls:MetroWindow.IconTemplate>
    <DockPanel FlowDirection="RightToLeft" LastChildFill="True">
        <Border Background="{DynamicResource AccentColorBrush}"
                BorderBrush="{DynamicResource AccentColorBrush}"
                BorderThickness="2,2,2,2"
                CornerRadius="20"
                Padding="8"
                Margin="5" DockPanel.Dock="left" >
            <Border.Effect>
                <DropShadowEffect Color="Silver" />
            </Border.Effect>
            <StackPanel Orientation="Vertical">
                <Button x:Name="btnSuppliers" Style="{DynamicResource defaultStyle}" Command="{Binding SuppliersCommand}" Click="btnSuppliers_Click">
                    <Button.Content>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <iconPacks:PackIconMaterial HorizontalAlignment="Center" Kind="Domain" Width="30" Height="30" />
                            <TextBlock Grid.Row="1" HorizontalAlignment="Center" Margin="1,3"><Run Text="الموردين"/></TextBlock>
                        </Grid>
                    </Button.Content>
                </Button>
                <Button x:Name="btnClients"  Command="{Binding ClientsCommand}" Style="{DynamicResource defaultStyle}" Click="btnClients_Click">
                    <Button.Content>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <iconPacks:PackIconFontAwesome HorizontalAlignment="Center" Kind="User" Width="30" Height="30" />
                            <TextBlock Grid.Row="1" HorizontalAlignment="Center" Margin="1,3"><Run Text="العملاء"/></TextBlock>
                        </Grid>
                    </Button.Content>
                </Button>
                <Button x:Name="btnItems"  Command="{Binding ItemsCommand}" Style="{DynamicResource defaultStyle}" Click="btnItems_Click">
                    <Button.Content>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <iconPacks:PackIconMaterial HorizontalAlignment="Center" Kind="LaptopChromebook" Width="30" Height="30" />
                            <TextBlock Grid.Row="1" HorizontalAlignment="Center" Margin="1,3" ><Run Text="الاصناف"/></TextBlock>
                        </Grid>
                    </Button.Content>
                </Button>
                <Button x:Name="btnSales"  Command="{Binding ReportsCommand}" Style="{DynamicResource defaultStyle}" Click="btnSales_Click">
                    <Button.Content>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <iconPacks:PackIconModern HorizontalAlignment="Center" Kind="CabinetFiles" Width="30" Height="30" />
                            <TextBlock Grid.Row="1" HorizontalAlignment="Center" Margin="1,3" ><Run Text="التقارير"/></TextBlock>
                        </Grid>
                    </Button.Content>
                </Button>
                <Button x:Name="btnSettings"  Command="{Binding SettingsCommand}" Style="{DynamicResource defaultStyle}">
                    <Button.Content>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <iconPacks:PackIconMaterial HorizontalAlignment="Center" Kind="Settings" Width="30" Height="30" />
                            <TextBlock Grid.Row="1" HorizontalAlignment="Center" Margin="1,3" ><Run Text="الاعدادات"/></TextBlock>
                        </Grid>
                    </Button.Content>
                </Button>
            </StackPanel>
        </Border>
        <Border  BorderBrush="{DynamicResource AccentColorBrush}"
                 Padding="3"
                BorderThickness="10,10,10,10"
                CornerRadius="20"
                Margin="5">
            <Controls:TransitioningContentControl Transition="LeftReplace"  prism:RegionManager.RegionName="{x:Static infrastrucure:RegionNames.MainContentRegion}"/>
        </Border>
    </DockPanel>
</Controls:MetroWindow>
